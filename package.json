{
  "name": "productivity-dashboard",
  "version": "1.0.0",
  "description": "A lightweight productivity dashboard with vanilla JavaScript",
  "scripts": {
    "test": "jest --verbose"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": ["**/__tests__/**/*.test.js"]
  }
}
